import { HighlightYelloDirectiveDirective } from './highlight-yello-directive.directive';

describe('HighlightYelloDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightYelloDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
